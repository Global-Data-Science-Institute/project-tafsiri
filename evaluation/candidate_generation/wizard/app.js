"use strict";
const app=document.querySelector("#app"), modal=document.querySelector("#modal"), modalBody=document.querySelector("#modalBody");
let data, screen="welcome", index=0, draft={}, saveTimer=null;
const decisions={ACCEPT_SIMPLE:["One Clear Meaning","The entry expresses one reasonably clear concept."],ACCEPT_VARIANT:["Variant of Another Form","The meaning is shared with another spelling, noun class, or grammatical form."],SPLIT_SENSES:["Contains Multiple Meanings","The entry contains meanings that should probably be represented separately."],NEEDS_EXPERT:["Needs More Linguistic Review","The evidence is unclear or specialist knowledge is needed."]};
async function api(path,body){const r=await fetch(path,{method:body?"POST":"GET",headers:body?{"Content-Type":"application/json"}:{},body:body?JSON.stringify(body):undefined});const x=await r.json();if(!r.ok)throw new Error(x.error||"Local application error");return x}
const esc=x=>String(x??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
function instructions(){return `<h1>Review Instructions</h1><p>You are helping Project Tafsiri understand the meanings recorded in a <strong>Luwanga</strong> dictionary. Judge the language evidence—not the computer system.</p><ol><li>Read the Luwanga word and English entry.</li><li>Decide whether it has one clear meaning, is a variant, contains multiple meanings, or needs expert review.</li><li>Improve the label or definition only when helpful.</li><li>Assess Tafsiri’s preliminary classification.</li><li>Save and continue.</li></ol>${guide()}<div class=notice><strong>Do not create or invent a meaning merely to complete the review.</strong> If the dictionary evidence and your linguistic knowledge are insufficient, select NEEDS_EXPERT. Preserve Luwanga-specific meaning; do not assume a Luwanga word is universal across Luhya dialects.</div><p>Work is saved only on this computer. You may close the browser and resume later. Complete all required choices before exporting.</p>`}
function guide(){return `<div class=guide-grid>${Object.entries(decisions).map(([k,v])=>`<div class=guide><span class=eyebrow>${k}</span><h3>${v[0]}</h3><p>${v[1]}</p></div>`).join("")}</div>`}
function setScreen(name){screen=name;render();window.scrollTo(0,0)}
function welcome(){const done=data.summary.completed;return `<section class="screen hero"><span class=eyebrow>Human Evaluation 001</span><h1>Project Tafsiri</h1><h2>Luwanga Dictionary Semantic Review</h2><p class=lead>You are helping determine what existing Luwanga dictionary entries mean. Focus on the language itself—not on judging the computer system.</p><p>There are 150 records. Work saves locally, and you may stop and resume later.</p><div class=button-row><button class=primary onclick="setScreen('how')">${done?'Start New Session':'Begin Review'}</button>${done?`<button class=secondary onclick="index=${data.state.last_index||0};setScreen('record')">Resume Existing Review — ${done} completed</button><button class=danger onclick="startOver()">Start Over</button>`:""}</div></section>`}
function how(){return `<section class=screen><span class=eyebrow>Before you begin</span><h1>How This Review Works</h1><div class=card><ol><li>Read the Luwanga word.</li><li>Read the English dictionary definition.</li><li>Choose the best meaning decision.</li><li>Improve the concept label or definition if useful.</li><li>Add helpful linguistic or cultural notes.</li><li>Say whether Tafsiri’s first classification seems reasonable.</li><li>Click <strong>Save & Next</strong>.</li></ol><p>There are no wrong answers when genuine linguistic uncertainty exists. Choosing <strong>Needs More Linguistic Review</strong> is better than guessing.</p><div class=notice>Do not invent modern translations or unsupported meanings. Keep the judgment specific to Luwanga rather than generalizing it to every Luhya dialect.</div></div><div class=button-row><button class=quiet onclick="setScreen('welcome')">Previous</button><button class=primary onclick="setScreen('guide')">Continue</button></div></section>`}
function guideScreen(){return `<section class=screen><span class=eyebrow>Decision guide</span><h1>Choose the Best Description</h1>${guide()}<div class=card><p><strong>Examples:</strong> <em>ameno → teeth</em> may illustrate one clear meaning. <em>amaatsi / amatsi</em> illustrates the kind of spelling question where “Variant” may apply—but only the human reviewer should decide whether they truly are variants.</p></div><div class=button-row><button class=quiet onclick="setScreen('how')">Previous</button><button class=primary onclick="setScreen('reviewer')">Continue</button></div></section>`}
function reviewer(){const r=data.state.reviewer||{};return `<section class=screen><span class=eyebrow>About the reviewer</span><h1>Reviewer Information</h1><p>All fields are optional and remain only on this computer.</p><div class=card><label class=field>Name<input id=rName value="${esc(r.name)}"></label><label class=field>Reviewer code or initials<input id=rCode value="${esc(r.code)}"></label><label class=field>Date<input id=rDate type=date value="${esc(r.date)}"></label><label class=field>Dialect expertise<input id=rDialect value="${esc(r.dialect_expertise)}" placeholder="For example: Native Luwanga speaker"></label><label class=field>Optional notes<textarea id=rNotes>${esc(r.notes)}</textarea></label></div><div class=button-row><button class=quiet onclick="setScreen('guide')">Previous</button><button class=primary onclick="saveReviewer()">Begin Records</button></div></section>`}
function annotation(id){return data.annotations[id]||{dictionary_entry_id:id,reviewer_decision:"",reviewer_concept_label:"",reviewer_definition:"",reviewer_notes:"",pipeline_bucket_correct:"",pipeline_ambiguity_correct:""}}
function progress(){const done=data.summary.completed,p=Math.round(done/150*100);return `<div class=progress-wrap><div class=progress-meta><strong>Record ${index+1} of 150</strong><span>Reviewed: ${done} · Remaining: ${150-done} · ${p}%</span></div><div class=bar><i style="width:${p}%"></i></div></div>`}
function record(){const r=data.records[index],a=annotation(r.dictionary_entry_id);draft={...a};const others=r.other_definitions_for_form||[];return `<section>${progress()}<div class=card><span class=eyebrow>Luwanga word</span><h1 class=word>${esc(r.word)}</h1><span class=eyebrow>English dictionary entry</span><p class=definition>${esc(r.english_definition)}</p><div class=facts><span class=pill>Normalized: ${esc(r.word_normalized)||'—'}</span><span class=pill>Part of speech: ${esc(r.part_of_speech)||'—'}</span><span class=pill>Noun class: ${esc(r.noun_class)||'—'}</span><span class=pill>Dialect: ${esc(r.dialect)}</span></div></div>${others.length?`<div class=notice><strong>Other meaning recorded for this same form:</strong><ul>${others.map(x=>`<li>${esc(x)}</li>`).join("")}</ul><span class=muted>These meanings are shown for comparison. Decide for yourself whether they are related.</span></div>`:""}<div class=card><div class=button-row><h2>Your Decision</h2><button class=secondary onclick="showHelp()">How do I decide?</button></div><div class=decision-grid>${Object.entries(decisions).map(([k,v])=>`<div class=decision><input type=radio name=decision id=${k} value=${k} ${a.reviewer_decision===k?'checked':''}><label for=${k}><strong>${v[0]}</strong><br><span class=muted>${v[1]}</span></label></div>`).join("")}</div><label class=field>Suggested Concept Label <span class=muted>Optional short English label, such as Teeth, Water, or Debt.</span><input id=concept value="${esc(a.reviewer_concept_label)}"></label><label class=field>Preferred Definition <span class=muted>Optionally write a clearer concise English definition.</span><textarea id=definition>${esc(a.reviewer_definition)}</textarea></label><label class=field>Reviewer Notes <span class=muted>Add helpful linguistic, cultural, grammatical, or contextual information.</span><textarea id=notes>${esc(a.reviewer_notes)}</textarea></label><h3>Was Tafsiri's bucket/classification reasonable?</h3>${choice('bucket',a.pipeline_bucket_correct)}<h3>Was Tafsiri correct to flag or not flag ambiguity?</h3>${choice('ambiguity',a.pipeline_ambiguity_correct)}<p id=validation class=validation></p><details><summary>Tafsiri's Initial Analysis — Advanced Details</summary><div class=notice>This automated preliminary assessment is shown only for comparison. It should not override your linguistic judgment. The heuristic score is a rule-based review priority score, not a probability.</div><dl><dt>Bucket/status</dt><dd>${esc(r.bucket)} / ${esc(r.mapping_status)}</dd><dt>Ambiguity action</dt><dd>${esc(r.ambiguity_action)||'None'}</dd><dt>Heuristic score</dt><dd>${Number(r.heuristic_confidence).toFixed(2)}</dd><dt>Proposed key</dt><dd>${esc(r.proposed_concept_key)||'None'}</dd><dt>Repeated form count</dt><dd>${r.repeated_form_count}</dd><dt>Distinct definitions</dt><dd>${r.distinct_definition_count}</dd><dt>Flags</dt><dd>${esc((r.ambiguity_indicators||[]).join(', ')||(r.heuristic_flags||[]).join(', '))}</dd></dl></details></div>${navigator()}<div class=button-row><button class=quiet onclick="navigate(-1)">Previous</button><button class=secondary onclick="save(false)">Save</button><button class=secondary onclick="nextUnreviewed()">Next Unreviewed</button><button class=primary onclick="save(true)">Save & Next</button><button class=quiet onclick="exitLater()">Exit / Return Later</button><label>Go to record # <input id=goto type=number min=1 max=150 style="width:80px" onchange="goTo(this.value)"></label></div></section>`}
function choice(name,value){return `<div class=choice-row>${['YES','NO','UNSURE'].map(x=>`<label><input type=radio name=${name} value=${x} ${value===x?'checked':''}>${x==='YES'?'Yes':x==='NO'?'No':'Unsure'}</label>`).join('')}</div>`}
function navigator(){return `<details><summary>Compact record navigator</summary><div class=navigator>${data.records.map((r,i)=>{const a=data.annotations[r.dictionary_entry_id],c=!a?'':a.reviewer_decision==='NEEDS_EXPERT'?'expert':a.reviewer_decision==='SPLIT_SENSES'?'split':isComplete(a)?'done':'';return `<button class="nav-dot ${c}" onclick="goTo(${i+1})" title="Record ${i+1}">${i+1}</button>`}).join('')}</div></details>`}
function collect(){const pick=n=>document.querySelector(`input[name=${n}]:checked`)?.value||"";return {...annotation(data.records[index].dictionary_entry_id),reviewer_decision:pick('decision'),reviewer_concept_label:document.querySelector('#concept').value,reviewer_definition:document.querySelector('#definition').value,reviewer_notes:document.querySelector('#notes').value,pipeline_bucket_correct:pick('bucket'),pipeline_ambiguity_correct:pick('ambiguity'),last_index:index}}
async function persistCurrent(){clearTimeout(saveTimer);const a=collect();const x=await api('/api/annotation',a);data.annotations[a.dictionary_entry_id]=x.annotation;data.summary=x.summary;return x}
async function save(next){try{await persistCurrent();if(next){if(index<149){index++;render()}else setScreen('summary')}else render()}catch(e){document.querySelector('#validation').textContent=e.message}}
function isComplete(a){return !!(a&&a.reviewer_decision&&a.pipeline_bucket_correct&&a.pipeline_ambiguity_correct)}
async function navigate(delta){await save(false);index=Math.max(0,Math.min(149,index+delta));render()} async function goTo(n){await save(false);index=Math.max(0,Math.min(149,Number(n)-1));render()}
async function nextUnreviewed(filter){if(screen==='record'){try{await persistCurrent()}catch(e){document.querySelector('#validation').textContent=e.message;return}}const start=index+1;for(let n=0;n<150;n++){const i=(start+n)%150,a=data.annotations[data.records[i].dictionary_entry_id];if(filter==='expert'?a?.reviewer_decision==='NEEDS_EXPERT':filter==='split'?a?.reviewer_decision==='SPLIT_SENSES':!isComplete(a)){index=i;setScreen('record');return}}setScreen('summary')}
async function exitLater(){await save(false);setScreen('welcome')}
async function saveReviewer(){const reviewer={name:rName.value,code:rCode.value,date:rDate.value,dialect_expertise:rDialect.value,notes:rNotes.value};data.state=await api('/api/state',{reviewer,last_index:index});setScreen('record')}
async function startOver(){if(prompt('Type START OVER to confirm. A backup will be created first.')!=='START OVER')return;await api('/api/start-over',{confirmation:'START OVER'});data=await api('/api/bootstrap');index=0;setScreen('how')}
function summaryScreen(){const s=data.summary,d=s.decision_counts;return `<section class=screen><span class=eyebrow>Completion review</span><h1>Review Summary</h1><div class=summary-grid><div class=metric><strong>Total</strong><br>${s.total}</div><div class=metric><strong>Completed</strong><br>${s.completed}</div><div class=metric><strong>Incomplete</strong><br>${s.incomplete}</div></div><div class=card><h2>Decisions</h2>${Object.entries(decisions).map(([k,v])=>`<p><strong>${v[0]}:</strong> ${d[k]||0}</p>`).join('')}</div>${s.incomplete?`<p class=validation>${s.incomplete} records still need the decision and both Tafsiri assessment questions.</p>`:''}<div class=button-row><button class=secondary onclick="nextUnreviewed()">Review Incomplete Records</button><button class=secondary onclick="nextUnreviewed('expert')">Review NEEDS_EXPERT</button><button class=secondary onclick="nextUnreviewed('split')">Review SPLIT_SENSES</button><button class=primary onclick="finish()">Finish Review</button></div><p id=validation class=validation></p></section>`}
async function finish(){if(data.summary.incomplete){document.querySelector('#validation').textContent='Complete all required choices before export.';return}setScreen('export')}
function exportScreen(){return `<section class=screen><span class=eyebrow>Final step</span><h1>Export Completed Review</h1><div class=card><p>Your completed review will be saved locally as CSV, JSONL, and a summary JSON report. Existing completed exports are backed up automatically.</p><button class=primary onclick="doExport()">Export Completed Review</button><p id=exportResult></p></div></section>`}
async function doExport(){try{const x=await api('/api/export',{});exportResult.innerHTML='<strong>Export complete.</strong><br>'+x.files.map(esc).join('<br>')}catch(e){exportResult.textContent=e.message}}
function showHelp(){modalBody.innerHTML=instructions();modal.classList.remove('hidden')}function render(){document.querySelector('#headerStep').textContent=screen==='record'?`Record ${index+1} of 150`:'';app.innerHTML=screen==='welcome'?welcome():screen==='how'?how():screen==='guide'?guideScreen():screen==='reviewer'?reviewer():screen==='record'?record():screen==='summary'?summaryScreen():exportScreen();app.focus()}
instructionsBtn.onclick=showHelp;closeModal.onclick=()=>modal.classList.add('hidden');modal.onclick=e=>{if(e.target===modal)modal.classList.add('hidden')};document.addEventListener('keydown',e=>{if(e.key==='Escape')modal.classList.add('hidden')});
app.addEventListener('input',()=>{if(screen!=='record')return;clearTimeout(saveTimer);saveTimer=setTimeout(()=>persistCurrent().catch(()=>{}),800)});
app.addEventListener('change',()=>{if(screen!=='record')return;clearTimeout(saveTimer);saveTimer=setTimeout(()=>persistCurrent().catch(()=>{}),150)});
(async()=>{data=await api('/api/bootstrap');index=data.state.last_index||0;render()})().catch(e=>app.innerHTML=`<h1>Unable to start</h1><p>${esc(e.message)}</p>`);
