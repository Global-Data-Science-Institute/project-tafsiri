"use client";import{useState}from"react";export default function Page(){const[msg,setMsg]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const body=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/admin/invitations",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const j=await r.json();setMsg(j.message||"Request failed")}return <main className="shell"><div className="card"><h1>Invite reviewer</h1><form onSubmit={submit}><label>Email (Auth only)</label><input name="email" type="email" required/><label>Display name (optional)</label><input name="displayName"/><label>Reviewer code</label><input name="reviewerCode" pattern="LUW-R[0-9]{3}" placeholder="LUW-R001" required/><input type="hidden" name="studyKey" value="HUMAN_EVALUATION_001_LUWANGA"/><button>Preview invitation</button><p aria-live="polite">{msg}</p></form></div></main>}
